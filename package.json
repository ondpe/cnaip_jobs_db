{
  "name": "job-scraper-api",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "pip3 install -r requirements.txt",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0",
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0"
  }
}